package com.chesko.stream_pro_tv.ui.theme

import androidx.compose.ui.graphics.Color

// Obsidian Gold Palette for TV
val UniversePrimary = Color(0xFFFACC15) // Amber Gold
val UniverseSecondary = Color(0xFFEAB308) // Deep Gold
val UniverseTertiary = Color(0xFFCA8A04) // Darker Gold

// Backgrounds & Surfaces
val UniverseBackground = Color(0xFF000000) // True Black
val UniverseSurface = Color(0xFF111111) // Deep Charcoal
val UniverseSurfaceVariant = Color(0xFF1A1A1A) // Dark Grey

// Neutrals & Accents
val StarlightWhite = Color(0xFFE5E7EB) // Grayish White
val NebulaGlow = Color(0xFFFACC15).copy(alpha = 0.15f)
val DeepSpaceBlue = Color(0xFF171717)
val GalacticError = Color(0xFFEF4444) // Modern Red

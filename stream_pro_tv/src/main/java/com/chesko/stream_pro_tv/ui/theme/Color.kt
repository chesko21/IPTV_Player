package com.chesko.stream_pro_tv.ui.theme

import androidx.compose.ui.graphics.Color

// Universe Core Palette
val UniversePrimary = Color(0xFFBB86FC) // Cosmic Violet (Nebula Accent)
val UniverseSecondary = Color(0xFF03DAC6) // Cyan Starburst
val UniverseTertiary = Color(0xFF3700B3) // Deep Galactic Purple

// Backgrounds & Surfaces
val UniverseBackground = Color(0xFF00020A) // Absolute Space Black
val UniverseSurface = Color(0xFF0A0A0A) // Dark Void Surface
val UniverseSurfaceVariant = Color(0xFF1A1A1A) // Glassmorphism Base

// Neutrals & Accents
val StarlightWhite = Color(0xFFF5F5F5) // Soft White for text
val NebulaGlow = Color(0xFFBB86FC).copy(alpha = 0.15f)
val DeepSpaceBlue = Color(0xFF0D47A1)
val GalacticError = Color(0xFFCF6679) // Soft Red for error states
